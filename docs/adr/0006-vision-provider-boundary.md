# ADR 0006: Vision provider boundary

Food analysis will use a server-side provider interface. MVP analysis is synchronous and uses a deterministic mock provider; queue infrastructure and real providers are deferred.
