# Agent Instructions

- This repository currently has no commits or source files; do not assume a framework, package manager, or test command exists.
- Before implementing work, inspect the newly added project files and treat their manifests/configuration as the source of truth for setup, build, lint, typecheck, and test commands.
